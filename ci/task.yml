---
platform: linux

image_resource:
  type: docker-image
  source: 
    repository: python
    tag: alpine3.16

inputs:
- name: source-code

run:
  path: python
  args: ["pytest"]
  dir: source-code/ci